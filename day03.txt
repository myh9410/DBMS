hostname -> 내 호스트의 이름 알아보는 명령어
hostnamectl set-hostname ??? -> 호스트 이름 변경하는 명령어 ???이 바꿀 호스트 이름
@기준으로 왼쪽은 로그인한 이름 오른쪽은 컴퓨터 이름
exit -> 로그아웃

ICMP -> 통신 유무를 체크함
컴퓨터, 노트북, 핸드폰 등 디바이스들이 통신을 하기 위해서는 IP주소가 필요함.
사람이 택배 보낼 때 집 주소 알아야 택배 보내듯
GOOGLE 회사의 한 장비의 IP가 8.8.8.8이라는 IP사용.

#IP이 버전
IPv4 - x.x.x.x형태의 주소 사용 x마다 0~255중 하나의 수가 올 수 있음.
IPv6 - 
ICMP eco request 

Network is unreachable -> 통신이 안되고 있음.
ip address show -> 내 컴퓨터의 네트워크 어댑터(LAN카드)의 설정 정보

lo -> 루프백
루프백이라는 어댑터는 리눅스를 설치하면 자동으로 만들어지는 가상의 네트워크 어댑터
ens33 -> 네트워크 어댑터의 이름을 보여줌. ip주소가 보이지 않으면 ip주소가 부여되지 않았다는 것

리눅스를 처음 설치했을 때 기본적으로 네트워크 어댑터가 비활성화 상태임.

ifup ens33 <- interface up ens33
명령어 이용하면 ens33에 ip주소를 부여해 줌. <-inet 옆에 ip주소가 나옴.
ctrl + c -> 취소
ping 8.8.8.8누르면 ctrl+c 누를때 까지 계속 통신함.
reboot ->재부팅시키는 명령어

네트워크 어댑터를 활성화 시켜 놓아도 재부팅을 하면 다시 비활성화 상태가 됨.
컴퓨터를 재부팅할 때마다 ifup 명령어로 활성화를 계속시켜야함.
#ONBOOT기능
컴퓨터를 부팅할 때 네트워크 어댑터를 자동으로 활성화 시키는 기능임.
vi/etc/sysconfig/network-scripts/ipcfg-ens33

ifcfg-ens33이라는 파일이 존재함.
이파일은 ens





#스냅샷 : 가상 컴퓨터의 현재상태 저장
snapshot manager-> take snapshot name : 네트워크 설정 후(NAT)

P@ssw0rd


pwd -> 내가 현재 위치해 있는 곳이 어디인지 알려달라는 명령어
/root <-- 최상위에 있는 root라는 디렉터리
/ <----이게 최상위

cd <-- change directory
cd /etc/sysconfig

/etc/sysconfig
키보드의 tab키는 자동완성 시켜줌. tab키 연속 두번은 해당 디렉토리 내에서 이름을 가진 폴더들을 볼 수 있다.
ex) cd /etc/sysc 타이핑하고 tab 2번 누르면 etc디렉토리에서 sysc로 시작하는 파일들 이름 쭉 나열해줌.
리눅스의 자동완성은 지정한 문자로 시작하는 폴더가 딱 한개만 있을 때 사용할 수 있음.
root@dbms1 뒤에 sysconfig가 따라 나오는데 이는 현재 내 위치를 말해준다.

./ <---내 현재 위치	./는 생략가능 -> 생략하면 내 현재 위치부터 경로를 설정한다.
#리눅스의 경로 표기법
절대경로 표기법 : 최상위부터 경로를 지정
상대경로 표기법 : 내 현재 위치부터 경로를 지정
cd ../ <--한칸 상위 디렉토리로 이동
cd ../.. <--두칸 상위 디렉토리로 이동
ls /root 최상위 root에는 어떤 파일이 있는지 보여달라는 것
ls /root / -> 최상위에는 어떤 파일이 있는지, root에는 어떤 파일이 있는지 한번에 볼 수 있음
한번에 여러 경로를 볼 수 있다.



